{
  "name": "traitors",
  "version": "0.1.0",
  "description": "Traitors cast status",
  "scripts": {
    "download-images": "node src/download_images.js",
    "generate": "node src/generate_traitors_page.js",
    "build": "npm run download-images && npm run generate",
    "serve-dist": "npx serve dist"
  }
}
